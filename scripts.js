function showYear(year) {
    // This function will handle displaying the map and information for the specified year.
    // For now, it will just log the year to the console.
    console.log(`Showing information for the year ${year}`);
    // Future code will update the map with historical details and lines for the selected year.
}
